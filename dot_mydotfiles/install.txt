# New machine bootstrap notes

# 1. Install chezmoi
#    brew install chezmoi
#    or: sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply TravisTheTechie
#
# 2. Initialize and apply this repo
#    chezmoi init --apply TravisTheTechie
#
# The repo now contains:
# - managed config for nvim, zellij, VS Code, and Terminal theme files
# - a one-time macOS install script for brew packages, fonts, nvm, and the VS Code theme
# - an onchange macOS script that imports the TokyoNight Terminal.app profile and sets the font to Commit Mono
#
# Manual first-run step that still usually makes sense:
# SSH key
#   ssh-keygen -t ed25519 -C "travis@legomaster.net"
